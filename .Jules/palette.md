## 2026-01-17 - [Keyboard Accessibility for Canvas Buttons]
**Learning:** Custom `Canvas` buttons in Tkinter require explicit `takefocus=1` and key bindings (<Return>, <space>) to match native button behavior. They also need manual visual focus management.
**Action:** Always verify `takefocus` and keyboard bindings when creating or modifying custom interactive widgets.
