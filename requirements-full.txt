# Video Subtitle Toolkit - Full Requirements (with ASR)
# ======================================================
# Install with: pip install -r requirements-full.txt
# WARNING: This includes large ML libraries (~3GB download)

# Build tools
pyinstaller>=6.0

# ASR transcription (run_canary.py)
torch>=2.0
numpy>=1.24
soundfile>=0.12
nemo_toolkit[asr]>=1.20

# Note: ffmpeg must be installed separately
# Windows: choco install ffmpeg  OR  download from https://ffmpeg.org
# Linux: sudo apt install ffmpeg
# macOS: brew install ffmpeg
